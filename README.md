# TestRepo

A scratch repository used for testing git tooling.

## Contents

- `notes.txt` — freeform notes
- `colors.txt` — a list of colors
- `numbers.txt` — a list of numbers

## Usage

This repo has no build steps. It's just a collection of small text
files used to exercise git operations (branching, merging, diffing).
